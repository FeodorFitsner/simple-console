version: "{build}"

after_build:
  - ps: .\build.ps1
  - 'echo GitVersion_Version: %GitVersion_Version%'
  - 'echo GitVersion_Branch: %GitVersion_Branch%'
  - ps: import-module build-worker-api
  - ps: Add-AppveyorMessage 'Hello, there!'
  
test: off

branches:
  except:
    - gh-pages
    - release

#artifacts:
#  - path: SimpleConsole

#cache:
#  - packages
