os: default azure

install:
  #- ps: Start-Sleep -s 180
  - set TEST_VAR=Hello, world!
  - echo before reboot
  - ps: Restart-Computer -Force
  - ps: Start-Sleep -s 10

build_script:
  - echo after reboot
  - echo %TEST_VAR%

test: off
deploy: off
